import {
  BlockType,
  type TerrainParams,
  type BlockData,
} from "../types/terrain";

export function generateTopRight(p: TerrainParams): BlockData[] {
  const blocks: BlockData[] = [];

  return blocks;
}
