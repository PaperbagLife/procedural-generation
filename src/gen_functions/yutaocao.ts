import {
  BlockType,
  type TerrainParams,
  type GenFunction,
  type BlockData,
} from "../types/terrain";

export function generateBottomRight(p: TerrainParams): BlockData[] {
  const blocks: BlockData[] = [];

  return blocks;
}
